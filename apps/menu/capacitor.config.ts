import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'menu.futureforge.app',
  appName: 'menu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
